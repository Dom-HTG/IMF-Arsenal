import { GadgetRepository } from "../repository/gadget.repository"

export class GadgetHandler {
    // Dependency.
    private gadgetRepository: GadgetRepository = new GadgetRepository();

    public async RetriveGadgets() {
        try {
            
        } catch (e) {
            return resizeBy
        }
    };
    
}